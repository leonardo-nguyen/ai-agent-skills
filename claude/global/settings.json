{
  "theme": "dark",
  "outputStyle": "antigravity",
  "worktree": {
    "baseRef": "head"
  },
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"hookSpecificOutput\":{\"hookEventName\":\"UserPromptSubmit\",\"additionalContext\":\"Reminder: follow the active antigravity output style in this response - emoji-prefixed headings and bullets, blockquote callouts (NOTE/TIP/IMPORTANT/WARNING), tables instead of plain lists, file#Lx-y link format for code references, action-first structure. Apply this even to short or simple replies.\"}}'"
          }
        ]
      }
    ]
  },
  // NOTE: %USERPROFILE% assumes Claude Code runs hook/statusLine commands via cmd.exe on Windows. If a machine runs them via PowerShell instead, change to $env:USERPROFILE; on macOS/Linux, change to $HOME or ~.
  "statusLine": {
    "type": "command",
    "command": "node %USERPROFILE%/.claude/statusline.mjs"
  },
  "subagentStatusLine": {
    "type": "command",
    "command": "node %USERPROFILE%/.claude/subagent-statusline.mjs"
  }
}
